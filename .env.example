# Database (Optional - can also be configured per-user in Settings)
# This is the default/fallback database URL if users don't configure their own
DATABASE_URL="postgresql://username:password@host:5432/database?sslmode=require"

# For Neon (recommended):
# DATABASE_URL="postgresql://username:password@ep-xxx.us-east-2.aws.neon.tech:5432/neondb?sslmode=require"

# For local PostgreSQL (macOS):
# DATABASE_URL="postgresql://YOUR_USERNAME@localhost:5432/email_data_management"

# Multi-Database Mode:
# Users can configure their own database URLs in the Settings page.
# This allows multiple users to use their own separate databases.
# If a user hasn't configured a database URL, this default will be used.

# App
NEXT_PUBLIC_APP_NAME="Email Data Management"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
